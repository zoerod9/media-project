# MediaProjectVSCode
Java project for Media Mania inventory. 

## How to run in VS Code
- Open the folder in VS Code.
- Make sure Java extension pack is installed.
- Compile and run `Driver.java` (or run via the Run button).

The CSV `project1dataset.csv` is included at the project root.
# media-project
